# Codex Finding correction — QF-AI-COLLAB-v5

Use only Finding IDs and required changes in the trusted Review Result and
Organizer Disposition Record. Correct the smallest permitted surface. A claimed
fix is `FIX_CANDIDATE`, never `VERIFIED` or `CLOSED`, and must identify a new
candidate SHA and patch hash. Stop when scope, risk, budget or interpretation is
uncertain.
